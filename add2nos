#include<stdio.h>
int main()
{

int num1,num2,res;
printf("enter the two number\n");
scanf("%d %d", &num1,&num2);

res= (num1+num2);

printf("addition of two number is : ");
return(0);

}
int sum(int num1,int num2);
{
int num3;
num3=num1+num2;
return(num3);
}

